name: InstallAnsible
description: Install Ansible
schemaVersion: 1.0
phases:
  - name: build
    steps:
      - name: InstallAnsible
        action: ExecuteBash
        inputs:
          commands:
            - dnf install -y ansible-core unzip lvm2 openscap-scanner scap-security-guide
